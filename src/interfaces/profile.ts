export interface Profile {
    linkedinLink: string;       // URL to LinkedIn profile
    name: string;               // Full name of team member
    profileImagePath: string;   // Path to profile picture in cloud storage
    profileImage: string;       // Url to profile picture
    role: string;               // Role within the team
    team: string;               // Team name
}